name: Release Gemini Extension

on:
  push:
    tags:
      - 'nowledge-mem-gemini-cli-v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Resolve release metadata
        id: release_meta
        run: |
          VERSION=$(node -p "require('./package.json').version")
          EXPECTED_TAG="nowledge-mem-gemini-cli-v${VERSION}"
          if [ "${GITHUB_REF_NAME}" != "${EXPECTED_TAG}" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package version ${VERSION}" >&2
            exit 1
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "expected_tag=${EXPECTED_TAG}" >> "$GITHUB_OUTPUT"

      - name: Validate extension package
        run: npm run validate

      - name: Build release archive
        run: npm run package:release

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Nowledge Mem Gemini CLI v${{ steps.release_meta.outputs.version }}
          body_path: release-notes/${{ steps.release_meta.outputs.version }}.md
          files: |
            dist/nowledge-mem-gemini-cli.tar.gz
            dist/nowledge-mem-gemini-cli.tar.gz.sha256
