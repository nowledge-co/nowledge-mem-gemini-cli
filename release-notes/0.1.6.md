# Nowledge Mem Gemini CLI v0.1.6

This release improves session continuity around Gemini CLI context compression.

- Adds a `PreCompress` lifecycle hook so the extension imports the current Gemini thread before context compression.
- Keeps the existing session-end import path as a safety net.

After updating, start a fresh Gemini CLI session so the new hook is loaded.
